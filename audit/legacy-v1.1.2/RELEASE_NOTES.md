# Release notes for v1.1.2

Version v1.1.2 preserves the eleven public endpoint types from v1.1.1 and
adds two paper-facing endpoints:

- `paperEquationSixThree_exact` for all claims in equation (6.3); and
- `paperEquationSixSix_exact` for the identities and strict decrease in
  equation (6.6).

The public kernel audit therefore contains thirteen declarations. The new
endpoints only package the already proved source theorems into stable public
interfaces; no earlier public theorem type is changed.

This archive is licensed under `GPL-3.0-only`. The exact Lean, mathlib, Elan,
and Lake versions remain pinned as recorded in `ENVIRONMENT.md`. The release
evidence was regenerated by a clean verification and repeated after extracting
the final ZIP.

The version-specific Zenodo DOI is assigned by Zenodo when this exact archive
is published as a new version. The archive SHA-256 identifies the uploaded
bytes independently of that DOI.
