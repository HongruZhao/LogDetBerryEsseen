# Retired modules from the broader v1.1.3 interface

These twelve files are retained byte-for-byte for historical reference. They
are outside the active source and import closure of the three main results.
The full earlier source project remains available in the parent archives and
in Git history. `SHA256SUMS` records these preserved file contents.
